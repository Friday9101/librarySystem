@model IEnumerable<NEWLIBRARY.Models.STUDENTS>

@{
    ViewBag.Title = "Editlist";
    Layout = "~/Views/Sheared/Admin_LayOut.cshtml";
}




<table>
    <tr>

        <th>
            @Html.DisplayName("EDIT WHO?")
        </th>

    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>

                @Html.ActionLink(item.FirstName + " " + item.LastName, "Edit", "admin", new { id = item.ID }, null)
            </td>

        </tr>
    }

</table>


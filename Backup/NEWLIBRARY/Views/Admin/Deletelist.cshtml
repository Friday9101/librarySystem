@model IEnumerable<NEWLIBRARY.Models.STUDENTS>

@{
    ViewBag.Title = "Deletelist";
    Layout = "~/Views/Sheared/Admin_LayOut.cshtml";
}






<table>
    <tr>

        <th>
            @Html.DisplayName("DELETE WHO?")
        </th>

    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>

                @Html.ActionLink(item.FirstName + " " + item.LastName, "Delete", "admin", new { id = item.ID }, null)
            </td>

        </tr>
    }

</table>


